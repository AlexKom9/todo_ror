= form_with(model: todo_list, local: true) do |form|
  - if todo_list.errors.any?
    #error_explanation
      %h2
        = pluralize(todo_list.errors.count, "error")
        prohibited this todo_list from being saved:
      %ul
        - todo_list.errors.full_messages.each do |message|
          %li= message
  .form-group
    %label{'data-toggle': "tooltip", 'data-placement': "top", title: "Required field"}
      Title
      %span.text-danger *
    = form.text_field :title, class: 'form-control', required: true
  .form-group
    = form.label :description
    = form.text_area :description, class: 'form-control', rows: 5
  .actions.mt-4
    = form.submit class: "btn btn-primary mr-2"
